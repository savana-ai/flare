# Prompt templates directly using JSONRepository
prompt_store = JSONRepository("path/to/prompts.json")

# Content store directly using JSONRepository
content_store = JSONRepository("path/to/content.json")
